{
  "private": true,
  "scripts": {
    "server": "make local",
    "build": "mkdir -p build && node_modules/babel/bin/babel.js src/**/*.js -d build",
    "browserify": "mkdir -p dist && node_modules/browserify/bin/cmd.js build/src/main.js -o dist/out.js"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "browserify": "^11.0.1",
    "mocha": "^2.1.0"
  }
}
